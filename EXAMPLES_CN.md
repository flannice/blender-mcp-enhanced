# Blender MCP Enhanced - 中文自然语言建模示例
# 15条常用建模指令

## 基础几何体创建

### 1. 创建立方体
```
创建一个大立方体，放在场景中央，尺寸为3x3x3
```

### 2. 创建球体
```
添加一个半径为2的球体，位置在(0, 0, 2)
```

### 3. 创建复杂几何体
```
创建一个圆柱体，半径1，高4，放置在地面位置(5, 5, 0)
```

## 物体变换

### 4. 移动物体
```
将立方体移动到位置(3, 3, 1)
```

### 5. 旋转物体
```
把球体绕Z轴旋转45度
```

### 6. 缩放物体
```
将圆柱体X轴缩放到2倍
```

## 材质与渲染

### 7. 添加材质
```
给立方体添加一个蓝色金属材质，粗糙度0.2，金属度0.8
```

### 8. 设置透明度
```
创建一个透明玻璃材质，透明度0.5，应用于球体
```

### 9. 添加纹理
```
给地面添加木纹纹理贴图，并执行UV展开
```

## 灯光与相机

### 10. 添加灯光
```
在场景上方添加一个日光灯，颜色偏暖，强度500
```

### 11. 添加聚光灯
```
创建一个聚光灯，照射立方体，制造戏剧性效果
```

### 12. 设置相机
```
添加一个相机，位置在(0, -8, 5)，对准场景中心，焦距85mm
```

## 布尔运算与复制

### 13. 布尔并集
```
创建立方体和球体，进行布尔并集运算，生成新物体
```

### 14. 阵列复制
```
将圆柱体沿X轴方向阵列复制10个，间距2
```

### 15. 镜像复制
```
创建立方体的镜像副本，沿Y轴翻转
```

---

## 完整工作流示例

### 场景：创建一个带材质的积木房子

1. 清空场景
   ```
   新建场景
   ```

2. 创建墙体（立方体）
   ```
   创建一个立方体，命名为墙1，尺寸(4, 0.5, 3)，位置(0, 0, 1.5)
   ```

3. 创建门洞（布尔差集）
   ```
   创建一个小立方体，使用布尔差集在墙体上挖出门洞
   ```

4. 添加材质
   ```
   给墙体添加红色材质，粗糙度0.6，金属度0.1
   ```

5. 添加灯光
   ```
   添加日光灯，位置(5, -5, 8)，强度1000
   ```

6. 设置相机
   ```
   添加相机位置(0, -10, 5)，焦距50mm
   ```

7. 渲染输出
   ```
   导出PNG渲染图到桌面，文件名house_render.png
   ```

---

## 提示

- 所有尺寸单位为Blender默认单位（米）
- 位置坐标：[X轴前后, Y轴左右, Z轴上下]
- 旋转使用弧度制，如需使用度需转换
- 材质颜色使用RGB格式，值范围0-1